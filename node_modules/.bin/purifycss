#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../purify-css/bin/purifycss" "$@"
  ret=$?
else 
  node  "$basedir/../purify-css/bin/purifycss" "$@"
  ret=$?
fi
exit $ret
